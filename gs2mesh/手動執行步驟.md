# gs2mesh 手動執行步驟（在你自己登入的 Chrome）

因為 Google 封鎖自動化瀏覽器登入（chrome-devtools 帶 --enable-automation 旗標），
Colab 這一步要在你平常登入 Google 的 Chrome 裡做。以下每格可直接複製貼進 Colab。

## 前置：檔案
要上傳的素材：`~/Desktop/Blender_3DGS/工坊_標準3DGS.ply`（62MB, 110萬高斯）

---

## 步驟 1：開 Colab、設 A100
1. 你的 Chrome 開 https://colab.research.google.com/
2. 左上「檔案 → 新增筆記本」
3. 上方選單「執行階段 → 變更執行階段類型」→ 硬體加速器選 **A100 GPU** → 儲存
   （沒有 A100 選項就選 L4；免費版只有 T4，整場可能 OOM）

## 步驟 2：上傳 ply（二選一）

**方式 A（直接上傳，簡單但 62MB 偶爾會斷）**：左側資料夾圖示 → 上傳 → 選
`工坊_標準3DGS.ply`。上傳完檔案在 `/content/工坊_標準3DGS.ply`。

**方式 B（Drive 掛載，較穩）**：先把 ply 丟到你的 Google Drive，然後跑：
```python
from google.colab import drive
drive.mount('/content/drive')
# 路徑改成 /content/drive/MyDrive/你放的位置/工坊_標準3DGS.ply
```

---

## Cell 1 — 環境
```python
!pip -q install plyfile open3d
import numpy as np, open3d as o3d
from plyfile import PlyData
print("open3d", o3d.__version__, "| CUDA:", o3d.core.cuda.is_available())
```

## Cell 2 — 讀 3DGS + 濾飄絲高斯（整場，不裁切）
```python
PLY = "/content/工坊_標準3DGS.ply"   # ← Drive 掛載的話改成 Drive 路徑
OPACITY_MIN, SCALE_MAX = 0.3, 0.3

ply = PlyData.read(PLY); v = ply["vertex"]
xyz = np.c_[v["x"], v["y"], v["z"]].astype(np.float64)
C0 = 0.28209479177387814
rgb = np.clip(0.5 + C0*np.c_[v["f_dc_0"],v["f_dc_1"],v["f_dc_2"]], 0, 1)
opacity = 1/(1+np.exp(-v["opacity"]))
scale = np.exp(np.c_[v["scale_0"],v["scale_1"],v["scale_2"]]).max(1)
keep = (opacity>OPACITY_MIN) & (scale<SCALE_MAX)
print(f"總高斯 {len(xyz):,} → 濾後 {keep.sum():,} ({keep.mean()*100:.1f}%)")
xyz, rgb = xyz[keep], rgb[keep]
```

## Cell 3 — Poisson 取網格
```python
pcd = o3d.geometry.PointCloud()
pcd.points = o3d.utility.Vector3dVector(xyz)
pcd.colors = o3d.utility.Vector3dVector(rgb)
pcd.estimate_normals(o3d.geometry.KDTreeSearchParamHybrid(radius=0.1, max_nn=30))
pcd.orient_normals_consistent_tangent_plane(30)

mesh, dens = o3d.geometry.TriangleMesh.create_from_point_cloud_poisson(pcd, depth=10, n_threads=-1)
mesh.remove_vertices_by_mask(np.asarray(dens) < np.quantile(dens, 0.05))
print(f"Poisson: {len(mesh.vertices):,} 頂點 {len(mesh.triangles):,} 面")
```

## Cell 4 — 清理 + 匯出
```python
mesh.remove_duplicated_vertices(); mesh.remove_degenerate_triangles()
mesh.remove_unreferenced_vertices()
mesh = mesh.filter_smooth_taubin(number_of_iterations=5)
mesh.compute_vertex_normals()
o3d.io.write_triangle_mesh("workshop_mesh.obj", mesh)
o3d.io.write_triangle_mesh("workshop_mesh.ply", mesh)
print("已存 workshop_mesh.obj / .ply")
from google.colab import files
files.download("workshop_mesh.obj")   # 下載回本機
```

---

## 步驟 3：把 mesh 放回本機
下載的 `workshop_mesh.obj` 放到 `~/3d/scan2cad/gs2mesh/`，跟我說一聲，
我在本機做目視檢查（牆面/桌面是否乾淨、有無 3DGS 飄絲殘留的疙瘩），
再接下一環 QUADify 重拓樸。

## 如果 A100 OOM 或太慢
Cell 2 開頭加裁切（先跑工坊一角）：
```python
# 在 keep 之後加：
lo = xyz.min(0)
box = (xyz[:,0]<lo[0]+4)&(xyz[:,2]<lo[2]+4)   # 4×4m 一角
xyz, rgb = xyz[box], rgb[box]
```
